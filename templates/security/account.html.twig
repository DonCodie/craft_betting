<div class="account">
    <div class="account__popin">
        <div class="account__content">
            <div class="account__content-title">
                bienvenue {{ app.user.username }}
            </div>
            {% if app.user.vipStatus != 'default' %}
                <div class="account__content-vip">
                    statut VIP : <span
                            class="account__content-vip-status {{ app.user.vipStatus }}">{{ app.user.vipStatus }}</span>
                </div>
            {% endif %}
            <hr class="account__content-hr">
            <div class="account__content-info">
                - mes informations
            </div>
            <div class="account__content-info">
                - mon status
            </div>
            <div class="account__content-info">
                <a href="{{ path('security_logout') }}" class="account__content-link">
                    - déconnexion
                </a>
            </div>
        </div>
    </div>
</div>
